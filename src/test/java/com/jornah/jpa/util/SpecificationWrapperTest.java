package com.jornah.jpa.util;

import com.jornah.jpa.model.User;
import org.springframework.data.jpa.domain.Specification;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;

/**
 * @author licong
 * @date 2022/11/3 00:11
 */
public class SpecificationWrapperTest {
    public void testEqual() throws Exception {
//        Specification<User> specification = new SpecificationWrapper<User>().eq(true, User::getId, 1).buildSpecification();
//        JpaSpecificationExecutor<User> executor = null;
//        executor.findAll(specification);
    }
}
